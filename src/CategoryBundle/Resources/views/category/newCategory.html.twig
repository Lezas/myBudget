{% extends 'base.html.twig'  %}
{% block body %}
    <div class="col-md-6 col-md-offset-3 text-center">
        <div class="h2">New Category</div>
        {{ form_start(form, { 'attr' : {'class' : 'create_event_form'}}) }}

        {{ form_end(form) }}
    </div>
{% endblock body %}
