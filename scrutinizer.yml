checks:
    php:
        code_rating: true
        duplication: true

build:
    tests:
        override:
            -
                command: 'phpunit --coverage-clover=covarage.cml'
                coverage:
                    file: 'covarage.xml'
                    format: 'clover'